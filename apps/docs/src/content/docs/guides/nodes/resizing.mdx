---
title: Resizing
description: Understanding how resizing works in ngDiagram
---

import Configuration from '@components/angular/nodes/resizing/configuration/configuration.astro';
import Hierarchy from '@components/angular/nodes/resizing/hierarchy/hierarchy.astro';
import CodeSnippet from '@components/code-snippet/code-snippet.astro';

The **resizing** feature in ngDiagram allows users to interactively change the size of nodes on the diagram canvas.
Resizable nodes display resize handles and lines that users can drag to adjust the node's width and height.

The new size is reflected in the node's appearance and can be used to create flexible, dynamic diagrams.

## How It Works

- Nodes can be resized by dragging the resize handles or lines.
- The node's size is updated in its data model.
- Resizing can be enabled per node, configured globally via diagram config or in the resize adornment component of a custom node template.

## Global Configuration

Resizing is enabled by default. You can control the default resizing behavior for all nodes by setting the `defaultResizable` property in the `NgDiagramConfig`:

```typescript
import { NgDiagramConfig } from 'ng-diagram';

const config: NgDiagramConfig = {
  resize: {
    defaultResizable: false,
  },
};
```

## Default Nodes

Default nodes use the value from the global configuration to determine if they are resizable. You can override the global configuration for specific nodes by setting the `resizable` property on the node data.

```typescript
{
  id: 'node1',
  type: 'default',
  position: { x: 100, y: 100 },
  data: {
    resizable: false,
  },
}
```

## Custom Node Setup

To make a custom node resizable, wrap your node with `<ng-diagram-node-resize-adornment>` in your node template and import the `NgDiagramNodeResizeAdornmentComponent`.

<CodeSnippet relativePath="nodes/resizing/configuration/node/node.component.ts" />

In custom nodes, together with the global and data properties, the resizable state can also be controlled via the `defaultResizable` property on the `NgDiagramNodeResizeAdornmentComponent`.

```html
<ng-diagram-node-resize-adornment [defaultResizable]="true"></ng-diagram-node-resize-adornment>
```

## Hierarchy

The resizable state of a node is determined by the following hierarchy (from highest to lowest priority):

1. The `resizable` property on the node data.
2. The `defaultResizable` property on the `NgDiagramNodeResizeAdornmentComponent` (for custom nodes).
3. The global `defaultResizable` setting in `NgDiagramConfig`.

In the example below you can see how different nodes have different resizable states based on this hierarchy. Select a node to view its configuration.

<Hierarchy />

The above example uses this model:

<CodeSnippet relativePath="nodes/resizing/hierarchy/diagram.component.ts" sectionId="model" />

Setting the `NgDiagramNodeResizeAdornmentComponent` `defaultResizable` is done by using the `adornmentResizable` property in the nodes custom data:

<CodeSnippet relativePath="nodes/resizing/hierarchy/node/node.component.ts" />

## Customization

### Resize Handle

Here are the CSS variables you can use to style the resize handle:

```
  --ngd-resize-handle-background-color           // Background color of the handle
  --ngd-resize-handle-background-color-hover     // Background color on hover or focus
  --ngd-resize-handle-background-color-active    // Background color when active or resizing
  --ngd-resize-handle-size                      // Size (width and height) of the handle
  --ngd-resize-handle-top                       // Positioning of the handle
  --ngd-resize-handle-right                     // Positioning of the handle
  --ngd-resize-handle-bottom                    // Positioning of the handle
  --ngd-resize-handle-left                      // Positioning of the handle
```

### Resize Line

Here are the CSS variables you can use to style the resize line:

```
  --ngd-resize-line-border-width                 // Border width of the line
  --ngd-resize-line-border-style                 // Border style of the line (e.g. solid, dashed)
  --ngd-resize-line-border-color                 // Border color of the line
```

You can override these variables in your styles to customize the appearance and position of the resize handles and lines.

## Configuration

You can further customize resizing behavior using the `resize` configuration in `NgDiagramConfig`.
This configuration allows you to control minimum node size and other resizing options:

- **getMinNodeSize** – Returns the minimum size for a node during resizing.
- **allowResizeBelowChildrenBounds** – By default, groups can be resized to any size, even smaller than their children. If you want to prevent resizing a group smaller than its children (so all children always remain fully contained), set this option to `false`.

You can set these options in your diagram configuration and pass them to the `ng-diagram` component:

<CodeSnippet relativePath="nodes/resizing/configuration/diagram.component.ts" sectionId="config" />

## Example

<Configuration />

## Further Reading

[Nodes →](/docs/guides/nodes/nodes) | [Custom Nodes →](/docs/guides/nodes/custom-nodes)
